import Rectangle from './Rectangle';

describe("Rectangle", () => {
    const rectangle = new Rectangle(4, 5);

    describe("area()", () => {
        it("calculates the area based on the height and width", () => {
            throw new Error("...start here!");
        });
    });
});
